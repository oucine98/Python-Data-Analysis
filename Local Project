# 1. Initialize Git (if not already)
git init

# 2. Add the remote origin (replace with your GitHub URL)
git remote add origin https://github.com/YourUsername/tesla-gme-dashboard.git

# 3. Stage all files
git add .

# 4. Commit with a meaningful message
git commit -m "Initial commit: add notebook, README, requirements, gitignore"

# 5. Push to GitHub
git branch -M main
git push -u origin main
